# Assets — Semana 03

| Archivo | Muestra | Vinculado desde |
|---------|---------|-----------------|
| `01-propagacion-excepcion-stack.svg` | Pila Main → Import → ParseLine → Divide; 1ª pasada (buscar catch y evaluar `when`) y 2ª pasada (desenrollar ejecutando `finally`) | `1-teoria/01-excepciones.md` |
| `02-jerarquia-exception.svg` | Ramas de `System.Exception` agrupadas por qué hacer con cada una: uso de API, entorno, bugs; dónde encajan las propias | `1-teoria/02-excepciones-personalizadas.md` |
| `03-flujo-stream.svg` | Capas disco → `FileStream` → `StreamReader` → `JsonSerializer`, escritura inversa y patrón `.tmp` + `File.Move` | `1-teoria/04-system-io-y-streams.md` |

Tema dark `#0d1117`, acento `#512BD4`/`#7a5cf0`, sin degradés, sans-serif, `<title>` + `<desc>`.
